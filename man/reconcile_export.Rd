% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_export.R
\name{reconcile_export}
\alias{reconcile_export}
\title{Export reconciled data and tree to files}
\usage{
reconcile_export(
  x,
  data = NULL,
  tree = NULL,
  species_col = NULL,
  dir = ".",
  prefix = "reconciled",
  tree_format = c("nexus", "newick"),
  drop_unresolved = TRUE
)
}
\arguments{
\item{x}{A \code{reconciliation} object.}

\item{data}{A data frame to align. If \code{NULL}, only the tree and mapping
are written.}

\item{tree}{An \code{ape::phylo} object or file path. If \code{NULL}, only the
data and mapping are written.}

\item{species_col}{Character(1). Column name in \code{data} containing
species names. Auto-detected if \code{NULL}.}

\item{dir}{Character(1). Output directory. Created if it does not exist.
Default \code{"."}.}

\item{prefix}{Character(1). File name prefix. Default \code{"reconciled"}.}

\item{tree_format}{Character(1). Tree output format: \code{"nexus"} (default)
or \code{"newick"}.}

\item{drop_unresolved}{Logical. Drop unresolved species? Default \code{TRUE}.}
}
\value{
A named list of file paths (invisibly):
\verb{$data}, \verb{$tree}, \verb{$mapping}.
}
\description{
Applies a reconciliation and writes the aligned dataset, pruned tree,
and full mapping table to disk. This produces publication-ready files
with a documented provenance trail.
}
\examples{
\dontrun{
result <- reconcile_tree(my_data, my_tree)
files <- reconcile_export(result, data = my_data, tree = my_tree,
                           dir = "output", prefix = "pgls_ready")
files$data     # "output/pgls_ready_data.csv"
files$tree     # "output/pgls_ready_tree.nex"
files$mapping  # "output/pgls_ready_mapping.csv"
}

}
